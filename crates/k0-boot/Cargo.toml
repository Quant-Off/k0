[package]
name = "k0-boot"
version = "1.0.0"
edition.workspace = true
license.workspace = true

[dependencies]
sha2 = { version = "0.10.9", default-features = false }

[build-dependencies]
sha2 = { version = "0.10.9", default-features = false }
